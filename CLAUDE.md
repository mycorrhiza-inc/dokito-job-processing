- For the dokito project the main file describing how everything is glued together is at mycorrhiza/dokito-job-processing/flake.nix and consists of a playwright directory for handling javascript scraping, a rust-data-transforms library for doing compelx data transforms, and importantly a golang runner library that takes in compiled links to those two outputs and orchestrates everything